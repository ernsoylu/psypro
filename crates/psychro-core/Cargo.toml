[package]
name = "psychro-core"
description = "Psychrometric state, process, and chart-geometry calculations."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]

[dev-dependencies]
approx = "0.5"
# Validation only for now: grade the frees/rustprop humid-air path against the
# same ASHRAE reference values this crate's own implementation is held to.
frees-core = { path = "../../vendor/frees-wasm/crates/frees-core", features = [
    "rustprop-backend",
] }
